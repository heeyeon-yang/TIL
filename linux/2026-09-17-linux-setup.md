## 2026-09-17 
(이것이 리눅스다 개정4판 Part1 ~p.149)

### 한 일
- VMware Workstation에 Rocky Linux 서버 환경 구축
  - 서버(rocky10-server) 설치 및 고정 IP 설정
  - 클론(Full Clone)으로 서버B, 리눅스 클라이언트 구성
- Windows 클라이언트 설치 및 도메인 가입 설정
- 스냅샷 생성 및 DVD(설치 ISO) 제거

### 네트워크 설정 정보 (VMnet8 NAT)
- 네트워크 대역: 192.168.120.0/24
- 게이트웨이 / DNS: 192.168.120.2
- IP 할당: 서버(.128, 고정), 서버B(.129, 고정), 클라이언트(.130, DHCP)

### 주요 개념 정리
- DHCP 서버: 클라이언트 부팅 시 IP, 서브넷 마스크, 게이트웨이, DNS 자동 할당
- 게이트웨이: 외부 네트워크 대역으로 나가는 출구 주소
- DNS 서버: 도메인 이름을 IP 주소로 변환

### 트러블슈팅 / 메모
- 디스크 저장 방식: 단일 파일(Single file)로 선택 (기존 디스크 손상 방지)
- Firefox 아이콘 클릭 시 설치 페이지 노출 → dnf install firefox 확인 결과 이미 설치되어 있음
- root 자동 로그인 설정 → 일반 사용자 계정 생성 및 전환 (`useradd`, `usermod -aG wheel`, GDM 설정)
- Windows 11 ISO 다운로드 오류 → 반복 재시도로 해결
